# If the value is different between development and production env, it
# should be keeped wirte in this file.

# Also, all this file's key will be written in `process.env`.

PORT = 3000
NODE_ENV = production